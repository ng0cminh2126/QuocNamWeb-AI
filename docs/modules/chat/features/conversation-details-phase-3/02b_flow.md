# [BƯỚC 2B] Phase 3 Flow Diagram: File Preview Modal

> **Module:** Chat  
> **Feature:** File Preview Modal  
> **Version:** 3.0  
> **Status:** ✅ APPROVED - Ready for implementation  
> **Created:** 2026-01-08  
> **Dependencies:**
>
> - [01_requirements.md](./01_requirements.md) ✅ APPROVED
> - [02a_wireframe.md](./02a_wireframe.md) ⏳ PENDING

---

## 📋 Overview

Flow diagrams chi tiết cho File Preview Modal, bao gồm:

- Happy path: User preview PDF thành công
- Error scenarios: File not found, network errors
- Edge cases: Single page, image files
- API call sequences
- State transitions

---

## 🎯 Primary Flow: Preview Multi-Page PDF

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER JOURNEY                                │
└─────────────────────────────────────────────────────────────────────┘

[START]
  │
  │ User viewing chat conversation
  │ Message has PDF attachment
  │
  ▼
┌──────────────────────┐
│ Click on PDF file    │ ◄─── Click file name or icon
└──────────────────────┘
  │
  ▼
┌──────────────────────┐
│ Trigger onClick      │ ◄─── ChatMainContainer.tsx
│ handler              │
└──────────────────────┘
  │
  │ Open modal with fileId
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│ FilePreviewModal Opens                                           │
│ - Show modal container                                           │
│ - Display loading skeleton                                       │
│ - Lock body scroll                                               │
│ - Focus modal                                                    │
└──────────────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────────────┐
│ API Call #1: GET /api/Files/{id}/preview                        │
│ - Request first page                                             │
│ - Include Bearer token                                           │
└──────────────────────────────────────────────────────────────────┘
  │
  ├─────────────────────┬─────────────────────┐
  │ SUCCESS (200)       │ ERROR (404/500)     │
  ▼                     ▼                     ▼
┌─────────────────┐   ┌───────────────────┐
│ Parse Response  │   │ Show Error State  │
│ - Body: Image   │   │ - Error icon      │
│ - Headers:      │   │ - Error message   │
│   X-Total-Pages │   │ - Retry button    │
│   X-Current-Page│   └───────────────────┘
└─────────────────┘             │
  │                             │
  │ totalPages = 5              │ User clicks Retry
  │ currentPage = 1             │ │
  │                             │ └──┐
  ▼                             │    │
┌────────────────────────────┐  │    │
│ Render First Page          │  │    │
│ - Hide loading skeleton    │  │    │
│ - Show image               │  │    │
│ - Enable navigation        │  │    │
│   (if totalPages > 1)      │  │    │
│ - Disable Prev button      │  │    │
│ - Enable Next button       │  │    │
└────────────────────────────┘  │    │
  │                             │    │
  │ Display:                    │    │
  │ "Page 1 of 5"               │    │
  │                             │    │
  ▼                             └────┘
┌────────────────────────────┐  Loop back to API Call #1
│ User Interaction Zone      │
│ - View page content        │
│ - Scroll inside modal      │
│ - Decide next action       │
└────────────────────────────┘
  │
  ├───────────────┬──────────────┬──────────────┬──────────────┐
  │               │              │              │              │
  ▼               ▼              ▼              ▼              ▼
┌──────────┐  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Click    │  │ Click   │  │ Press    │  │ Press    │  │ Press    │
│ Next     │  │ Prev    │  │ Arrow →  │  │ Arrow ←  │  │ ESC      │
└──────────┘  └─────────┘  └──────────┘  └──────────┘  └──────────┘
  │               │              │              │              │
  │               │              │              │              │
  ▼               ▼              ▼              ▼              ▼
┌──────────────────────────────────────────────┐         ┌──────────┐
│ Navigate to Next Page                        │         │ Close    │
│ - currentPage++                              │         │ Modal    │
│ - Check cache first                          │         └──────────┘
│ - If cached: Display immediately             │              │
│ - If not cached: API call                    │              │
└──────────────────────────────────────────────┘              │
  │                                                           │
  │ currentPage = 2                                           │
  │                                                           │
  ▼                                                           ▼
┌────────────────────────────────────────────┐        ┌─────────────┐
│ API Call #2:                               │        │ [END]       │
│ GET /api/pdf/{fileId}/pages/2/render?dpi=300│       │ - Close     │
│ - Request page 2                            │        │   modal     │
│ - Include Bearer token                      │        │ - Restore   │
└────────────────────────────────────────────┘        │   focus     │
  │                                                    │ - Unlock    │
  ├─────────────┬──────────────┐                      │   scroll    │
  │ SUCCESS     │ ERROR        │                      └─────────────┘
  ▼             ▼              ▼
┌───────────┐ ┌────────────┐
│ Render    │ │ Show Error │
│ Page 2    │ │ Message    │
│ - Cache   │ │ - Retry    │
│ - Display │ │   option   │
└───────────┘ └────────────┘
  │
  │ Update UI:
  │ "Page 2 of 5"
  │ Enable both Prev/Next
  │
  ▼
┌────────────────────────────────┐
│ User continues navigation      │
│ - Can go to pages 1-5          │
│ - Cached pages load instantly  │
│ - New pages make API calls     │
└────────────────────────────────┘
  │
  │ User reaches last page (5)
  │
  ▼
┌────────────────────────────────┐
│ Page 5 State                   │
│ - Disable Next button          │
│ - Enable Prev button           │
│ - Display "Page 5 of 5"        │
└────────────────────────────────┘
  │
  │ User clicks Close or ESC
  │
  ▼
[END] ◄─── Modal closes, return to chat
```

---

## 🖼️ Flow: Preview Single Page / Image

```
[START]
  │
  │ User clicks on image file (JPG/PNG)
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ FilePreviewModal Opens                                   │
│ - Show loading skeleton                                  │
└──────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ API Call: GET /api/Files/{id}/preview                   │
└──────────────────────────────────────────────────────────┘
  │
  ├────────────────┬──────────────────┐
  │ SUCCESS        │ ERROR            │
  ▼                ▼                  ▼
┌────────────┐   ┌─────────────────┐
│ Parse      │   │ Show Error      │
│ Headers:   │   │ - File not found│
│ X-Total-   │   └─────────────────┘
│ Pages = 1  │
└────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ Render Image                                             │
│ - Hide loading                                           │
│ - Show image                                             │
│ - NO navigation footer (totalPages = 1)                  │
└──────────────────────────────────────────────────────────┘
  │
  │ Layout:
  │ ┌────────────────────┐
  │ │ Header + Close     │
  │ ├────────────────────┤
  │ │ Image (full width) │
  │ └────────────────────┘
  │ (No footer)
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ User Interaction                                         │
│ - View image                                             │
│ - Click Close or ESC                                     │
└──────────────────────────────────────────────────────────┘
  │
  ▼
[END]
```

---

## ⚠️ Error Flow: File Not Found

```
[START]
  │
  │ User clicks on file
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ API Call: GET /api/Files/{id}/preview                   │
└──────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ Response: 404 Not Found                                  │
│ {                                                        │
│   "type": "NotFound",                                    │
│   "title": "File not found",                             │
│   "status": 404                                          │
│ }                                                        │
└──────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ Error State UI                                           │
│ ┌────────────────────────────────────────────────────┐   │
│ │ File Preview                              [X]      │   │
│ ├────────────────────────────────────────────────────┤   │
│ │                                                    │   │
│ │              ⚠️                                    │   │
│ │                                                    │   │
│ │         File not found or deleted                  │   │
│ │                                                    │   │
│ │     The file you're trying to preview              │   │
│ │     no longer exists.                              │   │
│ │                                                    │   │
│ │         [Retry]  [Close]                           │   │
│ │                                                    │   │
│ └────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
  │
  ├──────────────┬──────────────┐
  │ Retry        │ Close        │
  ▼              ▼              ▼
┌──────────┐   ┌──────────┐
│ Re-fetch │   │ [END]    │
│ API      │   │ Close    │
│ (loop    │   │ modal    │
│  back)   │   └──────────┘
└──────────┘
```

---

## 🌐 Error Flow: Network Error

```
[START]
  │
  │ User clicks file
  │ Network is unstable
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ API Call: GET /api/Files/{id}/preview                   │
│ - Request sent                                           │
│ - Network timeout / connection error                     │
└──────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ Network Error Caught                                     │
│ error.message = "Network request failed"                 │
└──────────────────────────────────────────────────────────┘
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ Network Error UI                                         │
│ ┌────────────────────────────────────────────────────┐   │
│ │ File Preview                              [X]      │   │
│ ├────────────────────────────────────────────────────┤   │
│ │                                                    │   │
│ │              ⚠️                                    │   │
│ │                                                    │   │
│ │      Failed to load file preview                   │   │
│ │                                                    │   │
│ │   Please check your connection and try again       │   │
│ │                                                    │   │
│ │         [Retry]  [Close]                           │   │
│ │                                                    │   │
│ └────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
  │
  ├──────────────┬──────────────┐
  │ Retry        │ Close        │
  ▼              ▼              ▼
┌──────────┐   ┌──────────┐
│ Re-fetch │   │ [END]    │
│ API      │   └──────────┘
└──────────┘
```

---

## 🔄 State Transition Diagram

```
┌────────────────────────────────────────────────────────────────┐
│                    MODAL STATE MACHINE                         │
└────────────────────────────────────────────────────────────────┘

                    [CLOSED]
                       │
                       │ onClick file
                       ▼
                  ┌──────────┐
                  │ LOADING  │ ◄──────┐
                  └──────────┘        │
                       │              │
          ┌────────────┼────────────┐ │
          │            │            │ │
    API Success   API Error    Cancel│
          │            │            │ │
          ▼            ▼            ▼ │
    ┌─────────┐  ┌─────────┐  ┌────────┐
    │ SHOWING │  │  ERROR  │  │ CLOSED │
    │  PAGE   │  └─────────┘  └────────┘
    └─────────┘       │
         │            │ Retry
         │            └───────────────┘
         │
    ┌────┴─────┐
    │          │
Navigate    Close/ESC
    │          │
    ▼          ▼
┌──────────┐ ┌────────┐
│ LOADING  │ │ CLOSED │
│ NEW PAGE │ └────────┘
└──────────┘
    │
    │ Success
    ▼
┌──────────┐
│ SHOWING  │
│ NEW PAGE │
└──────────┘
```

**State Definitions:**

```typescript
type ModalState =
  | "CLOSED" // Modal not visible
  | "LOADING" // Fetching first page
  | "SHOWING_PAGE" // Displaying page content
  | "LOADING_NEW_PAGE" // Navigating to different page
  | "ERROR"; // Error state with retry option

interface ModalContext {
  fileId: string;
  fileName: string;
  totalPages: number;
  currentPage: number;
  imageCache: Map<number, string>; // pageNumber → imageURL
  error: Error | null;
}
```

---

## 🎬 Keyboard Navigation Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  KEYBOARD SHORTCUTS                         │
└─────────────────────────────────────────────────────────────┘

User presses key while modal is open
  │
  ├──────────┬──────────┬──────────┬──────────┬──────────┐
  │          │          │          │          │          │
  ESC        →          ←          ↑          ↓        TAB
  │          │          │          │          │          │
  ▼          ▼          ▼          ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ Close  │ │ Next   │ │ Prev   │ │ Prev   │ │ Next   │ │ Focus  │
│ Modal  │ │ Page   │ │ Page   │ │ Page   │ │ Page   │ │ Next   │
└────────┘ └────────┘ └────────┘ └────────┘ └────────┘ │Element │
                                                        └────────┘
                                                             │
                                                        Focus Order:
                                                        1. Close Btn
                                                        2. Prev Btn
                                                        3. Next Btn
```

**Keyboard Event Handler:**

```typescript
function handleKeyDown(e: KeyboardEvent) {
  switch (e.key) {
    case "Escape":
      onClose();
      break;

    case "ArrowRight":
    case "ArrowDown":
      if (currentPage < totalPages) {
        navigateToPage(currentPage + 1);
      }
      break;

    case "ArrowLeft":
    case "ArrowUp":
      if (currentPage > 1) {
        navigateToPage(currentPage - 1);
      }
      break;

    case "Tab":
      // Let browser handle focus management
      // But ensure focus stays inside modal
      trapFocus(e);
      break;
  }
}
```

---

## 📱 Mobile Swipe Gesture Flow (Future)

```
┌─────────────────────────────────────────────────────────────┐
│              SWIPE NAVIGATION (Future Phase)                │
└─────────────────────────────────────────────────────────────┘

User touches screen
  │
  ▼
┌──────────────┐
│ touchstart   │ ◄─── Record start position
└──────────────┘
  │
  │ User drags finger
  │
  ▼
┌──────────────┐
│ touchmove    │ ◄─── Track movement
└──────────────┘
  │
  │ Calculate deltaX
  │
  ▼
┌──────────────────────────────────────────────────────────┐
│ touchend                                                 │
│ - Calculate swipe distance                               │
│ - Calculate swipe velocity                               │
└──────────────────────────────────────────────────────────┘
  │
  ├─────────────────┬─────────────────┬──────────────────┐
  │                 │                 │                  │
  │ Swipe Left      │ Swipe Right     │ Small Movement   │
  │ (deltaX < -50)  │ (deltaX > 50)   │ (|deltaX| < 50)  │
  │                 │                 │                  │
  ▼                 ▼                 ▼                  ▼
┌──────────┐      ┌──────────┐     ┌──────────┐
│ Next     │      │ Previous │     │ No       │
│ Page     │      │ Page     │     │ Action   │
└──────────┘      └──────────┘     └──────────┘
```

**Note:** Swipe gestures marked as "Future" - not implemented in Phase 3.

---

## 🔄 Caching Strategy Flow

```
┌─────────────────────────────────────────────────────────────┐
│                    PAGE CACHING LOGIC                       │
└─────────────────────────────────────────────────────────────┘

User requests page N
  │
  ▼
┌──────────────────────┐
│ Check imageCache     │
│ cache.has(pageN)?    │
└──────────────────────┘
  │
  ├──────────────┬──────────────┐
  │ YES          │ NO           │
  ▼              ▼              ▼
┌────────────┐ ┌──────────────────────┐
│ Return     │ │ Fetch from API       │
│ cached     │ │ GET /render?page=N   │
│ image URL  │ └──────────────────────┘
└────────────┘   │
  │              │ Success
  │              ▼
  │            ┌──────────────────────┐
  │            │ Store in cache       │
  │            │ cache.set(N, imageURL)│
  │            └──────────────────────┘
  │              │
  └──────────────┴───────┐
                         ▼
                    ┌─────────┐
                    │ Render  │
                    │ Page    │
                    └─────────┘

Cache Lifecycle:
- Created: On modal open
- Populated: As user navigates
- Cleared: On modal close OR logout
- Max size: Unlimited (session-based)
```

**Cache Implementation:**

```typescript
const [imageCache, setImageCache] = useState<Map<number, string>>(new Map());

async function loadPage(pageNumber: number) {
  // Check cache
  if (imageCache.has(pageNumber)) {
    setCurrentPageImage(imageCache.get(pageNumber)!);
    return;
  }

  // Fetch from API
  setLoading(true);
  try {
    const imageBlob = await fetchPageImage(fileId, pageNumber);
    const imageURL = URL.createObjectURL(imageBlob);

    // Store in cache
    setImageCache((prev) => new Map(prev).set(pageNumber, imageURL));
    setCurrentPageImage(imageURL);
  } catch (error) {
    setError(error);
  } finally {
    setLoading(false);
  }
}

// Cleanup on unmount
useEffect(() => {
  return () => {
    // Revoke all object URLs
    imageCache.forEach((url) => URL.revokeObjectURL(url));
  };
}, [imageCache]);
```

---

## 🎯 Decision Tree: Show Navigation?

```
Modal opens with file
  │
  ▼
┌────────────────────────┐
│ Parse X-Total-Pages    │
│ header                 │
└────────────────────────┘
  │
  ▼
┌────────────────────────┐
│ totalPages value?      │
└────────────────────────┘
  │
  ├──────────────┬──────────────┬──────────────┐
  │              │              │              │
  │ = 1          │ > 1          │ undefined    │
  │ (Single)     │ (Multi)      │ (Error)      │
  │              │              │              │
  ▼              ▼              ▼              ▼
┌────────────┐ ┌────────────┐ ┌────────────┐
│ NO         │ │ YES        │ │ NO         │
│ Navigation │ │ Show Prev/ │ │ Navigation │
│ Footer     │ │ Next + Page│ │ (Error     │
│            │ │ Indicator  │ │  state)    │
└────────────┘ └────────────┘ └────────────┘

Layout Examples:

totalPages = 1:
┌──────────────┐
│ Header       │
├──────────────┤
│ Content      │
└──────────────┘
(No footer)

totalPages > 1:
┌──────────────┐
│ Header       │
├──────────────┤
│ Content      │
├──────────────┤
│ Navigation   │
└──────────────┘

totalPages = undefined:
┌──────────────┐
│ Header       │
├──────────────┤
│ Error State  │
└──────────────┘
```

---

## 📋 HUMAN CONFIRMATION

| Hạng Mục                                | Status       |
| --------------------------------------- | ------------ |
| Đã review user flows                    | ✅ Đã review |
| Đã review error scenarios               | ✅ Đã review |
| Đã review state transitions             | ✅ Đã review |
| **APPROVED để tạo implementation plan** | ✅ APPROVED  |

**HUMAN Signature:** [ĐÃ DUYỆT]  
**Date:** 2026-01-08  
**Date:** \***\*\_\_\*\***

> ⚠️ **CRITICAL: AI KHÔNG ĐƯỢC tạo implementation plan nếu chưa APPROVED**

---

**Created:** 2026-01-08  
**Next Step:** Await HUMAN approval → Create implementation plan (BƯỚC 4)
