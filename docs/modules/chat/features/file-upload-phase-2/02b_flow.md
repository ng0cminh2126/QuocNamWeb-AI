# [BƯỚC 2B] Flow Diagram - File Upload Phase 2

> **Feature:** Upload nhiều file & Hiển thị ảnh dạng grid  
> **Version:** 2.0.0  
> **Status:** ⏳ PENDING APPROVAL  
> **Created:** 2026-01-14  
> **Module:** chat

---

## 📋 Version History

| Version | Date       | Changes                               |
| ------- | ---------- | ------------------------------------- |
| 2.0.0   | 2026-01-14 | Initial flow for Phase 2 batch upload |

---

## 🔄 Flow Overview

Phase 2 introduces:

1. **Batch Upload Flow** - Upload multiple files in 1 API call
2. **Error Handling Flow** - Partial success, validation errors, network errors
3. **API Selection Logic** - Switch between single upload (Phase 1) and batch upload (Phase 2)

---

## FLOW-01: User Selects Files

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: Click [📎] or [🖼️] button                          │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ SYSTEM: Open file picker                                        │
│ - [📎] Button → Accept all file types                           │
│ - [🖼️] Button → Accept images only (image/*)                    │
│ - Allow multiple selection                                      │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: Select X files                                     │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
           ┌─────┴─────┐
           │ X files   │
           │ selected  │
           └─────┬─────┘
                 ▼
         ┌───────────────┐
         │ Validation    │
         └───────┬───────┘
                 ▼
         ┌───────────────────────────────────────────┐
         │ Check:                                    │
         │ 1. Total files ≤ 10                       │
         │ 2. Each file size ≤ 10MB                  │
         │ 3. Total batch size ≤ 50MB                │
         │ 4. Allowed file types                     │
         └───────┬───────────────────────────────────┘
                 ▼
         ┌───────┴───────┐
         │ Valid?        │
         └───┬───────┬───┘
            NO       YES
             │        │
             ▼        ▼
    ┌───────────┐  ┌──────────────────────┐
    │ Show      │  │ Add files to         │
    │ Error     │  │ selectedFiles state  │
    │ Toast     │  │                      │
    └───────────┘  └──────┬───────────────┘
                           ▼
                   ┌──────────────────────┐
                   │ Render FilePreview   │
                   │ component            │
                   └──────────────────────┘
```

**Validation Errors:**

| Condition               | Error Message                         |
| ----------------------- | ------------------------------------- |
| Total files > 10        | "Maximum 10 files per message"        |
| Any file size > 10MB    | "File '{name}' exceeds 10MB limit"    |
| Total batch size > 50MB | "Total files size exceeds 50MB limit" |
| Invalid file type       | "File type not allowed: {type}"       |

---

## FLOW-02: Upload Files (API Selection Logic)

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: Click [Send] button                                │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────────┐
         │ selectedFiles.    │
         │ length === 0?     │
         └───┬───────────┬───┘
            YES          NO
             │            │
             ▼            ▼
    ┌────────────────┐  ┌──────────────────────────┐
    │ Send message   │  │ Check files count        │
    │ without files  │  │                          │
    │ (if has text)  │  └──────┬───────────────────┘
    └────────────────┘         ▼
                       ┌───────────────────┐
                       │ files.length === 1?│
                       └───┬───────────┬───┘
                          YES          NO
                           │            │
                           ▼            ▼
              ┌────────────────────┐  ┌──────────────────────┐
              │ Use Phase 1:       │  │ Use Phase 2:         │
              │ POST /api/Files    │  │ POST /api/Files/batch│
              │ (single upload)    │  │ (batch upload)       │
              └──────┬─────────────┘  └──────┬───────────────┘
                     │                        │
                     └────────┬───────────────┘
                              ▼
                     ┌──────────────────┐
                     │ Set isUploading  │
                     │ = true           │
                     │                  │
                     │ Disable send btn │
                     └──────┬───────────┘
                            ▼
                     [See FLOW-03 or FLOW-04]
```

**Logic Summary:**

```typescript
if (selectedFiles.length === 0) {
  // Send text-only message (existing Phase 1)
  sendMessage({ content });
} else if (selectedFiles.length === 1) {
  // Single file upload (existing Phase 1)
  uploadFile(selectedFiles[0]);
} else {
  // Batch upload (NEW Phase 2)
  uploadFilesBatch(selectedFiles);
}
```

---

## FLOW-03: Single File Upload (Phase 1 - Existing)

```
┌─────────────────────────────────────────────────────────────────┐
│ POST /api/Files                                                  │
│ FormData: { file: File }                                         │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────┐
         │ Response?     │
         └───┬───────┬───┘
            200      4xx/5xx
             │        │
             ▼        ▼
    ┌────────────┐  ┌──────────────┐
    │ Success:   │  │ Error:       │
    │ { fileId,  │  │ Show toast   │
    │   fileName,│  │ Clear files  │
    │   ... }    │  │              │
    └──────┬─────┘  └──────────────┘
           ▼
    ┌────────────────────────┐
    │ Send message with      │
    │ attachments: [{        │
    │   fileId,              │
    │   fileName,            │
    │   fileSize,            │
    │   contentType          │
    │ }]                     │
    └──────┬─────────────────┘
           ▼
    ┌────────────────────────┐
    │ Clear selectedFiles    │
    │ Set isUploading = false│
    └────────────────────────┘
```

---

## FLOW-04: Batch File Upload (Phase 2 - NEW)

```
┌─────────────────────────────────────────────────────────────────┐
│ POST /api/Files/batch                                            │
│ FormData: { files: File[] } (2+ files)                           │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────┐
         │ Response?     │
         └───┬───────┬───┘
            200      4xx/5xx
             │        │
             ▼        ▼
    ┌────────────────┐  ┌──────────────────┐
    │ SUCCESS:       │  │ NETWORK ERROR:   │
    │ BatchUpload    │  │ - Show toast     │
    │ Result         │  │ - Auto retry 1x  │
    └──────┬─────────┘  │   (Decision)     │
           ▼            └──────────────────┘
   ┌───────────────────┐
   │ Parse response:   │
   │ {                 │
   │   totalFiles,     │
   │   successCount,   │
   │   failedCount,    │
   │   results: []     │
   │ }                 │
   └──────┬────────────┘
          ▼
   ┌──────────────────────┐
   │ Check successCount   │
   └──────┬───────────────┘
          ▼
   ┌──────┴──────┐
   │ Success?    │
   └──┬──────┬───┘
     ALL    PARTIAL/NONE
      │       │
      ▼       ▼
┌──────────┐ ┌────────────────────────┐
│ All OK   │ │ Partial/Full Failure   │
└────┬─────┘ └────────┬───────────────┘
     │                │
     └────────┬───────┘
              ▼
    [See FLOW-05: Process Results]
```

---

## FLOW-05: Process Batch Upload Results

```
┌─────────────────────────────────────────────────────────────────┐
│ Loop through results array:                                      │
│ results.forEach((result, index) => { ... })                      │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────────┐
         │ result.success?   │
         └───┬───────────┬───┘
            YES          NO
             │            │
             ▼            ▼
    ┌────────────────┐  ┌──────────────────────┐
    │ Store fileId   │  │ Mark file as failed  │
    │ for message    │  │ Show error in UI     │
    │ attachment     │  │                      │
    └──────┬─────────┘  └──────┬───────────────┘
           │                    │
           └────────┬───────────┘
                    ▼
            ┌───────────────────┐
            │ All results       │
            │ processed?        │
            └───┬───────────────┘
                YES
                 ▼
         ┌───────────────────────┐
         │ successCount > 0?     │
         └───┬───────────────┬───┘
            YES              NO
             │                │
             ▼                ▼
    ┌────────────────────┐  ┌──────────────────┐
    │ Send message with  │  │ Show error toast │
    │ successful files   │  │ Keep failed files│
    │ only               │  │ in preview       │
    └──────┬─────────────┘  └──────────────────┘
           ▼
    ┌────────────────────────┐
    │ Show toast:            │
    │ - All success:         │
    │   "Files uploaded"     │
    │ - Partial success:     │
    │   "X/Y files uploaded" │
    └──────┬─────────────────┘
           ▼
    ┌────────────────────────┐
    │ Clear successful files │
    │ Keep failed files      │
    │ (for retry - Phase 3)  │
    └────────────────────────┘
```

---

## FLOW-06: Send Message with Attachments

```
┌─────────────────────────────────────────────────────────────────┐
│ Prepare SendMessageRequest:                                      │
│ {                                                                │
│   conversationId,                                                │
│   content,                                                       │
│   attachments: [                                                 │
│     { fileId, fileName, fileSize, contentType },                 │
│     { fileId, fileName, fileSize, contentType },                 │
│     ...                                                          │
│   ]                                                              │
│ }                                                                │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ POST /api/messages                                               │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────┐
         │ Response?     │
         └───┬───────┬───┘
            201      4xx/5xx
             │        │
             ▼        ▼
    ┌────────────┐  ┌──────────────────┐
    │ SUCCESS:   │  │ ERROR:           │
    │ MessageDto │  │ - Show toast     │
    │            │  │ - Keep files in  │
    │            │  │   preview        │
    └──────┬─────┘  │ - User can retry │
           ▼        └──────────────────┘
    ┌────────────────────────┐
    │ Add message to UI      │
    │ Clear selectedFiles    │
    │ Clear input            │
    │ Set isUploading = false│
    │ Scroll to bottom       │
    └────────────────────────┘
```

---

## FLOW-07: Error Handling - Partial Success

**Scenario:** 3/5 files uploaded successfully

```
┌─────────────────────────────────────────────────────────────────┐
│ Upload Result:                                                   │
│ {                                                                │
│   totalFiles: 5,                                                 │
│   successCount: 3,                                               │
│   failedCount: 2,                                                │
│   results: [                                                     │
│     { index: 0, success: true, fileId: "...", ... },             │
│     { index: 1, success: true, fileId: "...", ... },             │
│     { index: 2, success: false, error: "File too large" },       │
│     { index: 3, success: true, fileId: "...", ... },             │
│     { index: 4, success: false, error: "Invalid type" }          │
│   ]                                                              │
│ }                                                                │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ SYSTEM ACTION:                                                   │
│ 1. Show toast: "⚠️ 3/5 files uploaded. 2 files failed."          │
│ 2. Update FilePreview:                                           │
│    - Files 0,1,3 → Show checkmark ✓                             │
│    - Files 2,4 → Show error state with red border               │
│ 3. Send message with 3 successful files                          │
│ 4. Keep 2 failed files in preview (for retry - Phase 3)          │
└─────────────────────────────────────────────────────────────────┘

UI State:
┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐
│ file1.pdf [✓]│  │ file2.jpg [✓]│  │ file3.pdf         [!]│ ← Failed
└──────────────┘  └──────────────┘  │ 15MB         [↻] [×]│
                                    │ File too large       │
┌──────────────┐  ┌──────────────────────┐ └──────────────────┘
│ file4.jpg [✓]│  │ file5.mp4         [!]│ ← Failed
└──────────────┘  │ Invalid type [↻] [×]│
                  └──────────────────────┘
```

---

## FLOW-08: Error Handling - Network Error with Retry

```
┌─────────────────────────────────────────────────────────────────┐
│ POST /api/Files/batch                                            │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────────┐
         │ Network Error     │
         │ (timeout, 500)    │
         └──────┬────────────┘
                ▼
         ┌──────────────────┐
         │ Auto Retry?      │
         │ (1 time only)    │
         └──┬───────────┬───┘
           YES          NO (already retried)
            │            │
            ▼            ▼
    ┌────────────────┐  ┌──────────────────────┐
    │ Wait 2s        │  │ Show error toast:    │
    │ Retry upload   │  │ "Upload failed.      │
    │                │  │  Please try again."  │
    └──────┬─────────┘  │                      │
           ▼            │ Keep files in preview│
    [Back to FLOW-04]  └──────────────────────┘
```

**Retry Logic:**

```typescript
const uploadFilesBatch = useMutation({
  mutationFn: uploadFilesBatchAPI,
  retry: 1, // Auto retry once
  retryDelay: 2000, // Wait 2s before retry
  onError: (error) => {
    toast.error("Upload failed. Please try again.");
  },
});
```

---

## FLOW-09: Remove File from Preview

```
┌─────────────────────────────────────────────────────────────────┐
│ USER ACTION: Click [×] remove button on file preview item       │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ SYSTEM ACTION:                                                   │
│ 1. Remove file from selectedFiles state                          │
│ 2. Revoke blob URL if exists (cleanup)                           │
│ 3. Re-render FilePreview component                               │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────────┐
         │ selectedFiles     │
         │ .length === 0?    │
         └───┬───────────┬───┘
            YES          NO
             │            │
             ▼            ▼
    ┌────────────────┐  ┌──────────────────┐
    │ Hide           │  │ Show remaining   │
    │ FilePreview    │  │ files            │
    │ component      │  │                  │
    └────────────────┘  └──────────────────┘
```

---

## FLOW-10: Image Grid Display Logic

```
┌─────────────────────────────────────────────────────────────────┐
│ Receive MessageDto with attachments[]                            │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ Parse attachments:                                               │
│ const images = attachments.filter(isImage);                      │
│ const files = attachments.filter(isNotImage);                    │
└────────────────┬────────────────────────────────────────────────┘
                 ▼
         ┌───────────────────┐
         │ images.length?    │
         └───┬───────────┬───┘
            === 1        >= 2
             │            │
             ▼            ▼
    ┌────────────────┐  ┌──────────────────┐
    │ Single Image:  │  │ Image Grid:      │
    │ <MessageImage  │  │ <ImageGrid       │
    │   fileId={...} │  │   images={...}   │
    │   ...          │  │   ...            │
    │ />             │  │ />               │
    │ (existing)     │  │ (NEW)            │
    └──────┬─────────┘  └──────┬───────────┘
           │                    │
           └────────┬───────────┘
                    ▼
         ┌──────────────────────┐
         │ files.length > 0?    │
         └───┬──────────────┬───┘
            YES              NO
             │                │
             ▼                ▼
    ┌────────────────────┐  ┌──────────────┐
    │ Render file list   │  │ Done         │
    │ below grid/image   │  │              │
    └────────────────────┘  └──────────────┘
```

**Component Tree:**

```
MessageBubbleSimple
├─ (content text if exists)
├─ (if images.length === 1)
│  └─ MessageImage (existing)
├─ (if images.length >= 2)
│  └─ ImageGrid (NEW)
│     └─ ImageGridItem × N (NEW)
└─ (if files.length > 0)
   └─ FileAttachmentList
      └─ FileAttachment × N (existing)
```

---

## 🔧 State Management

### Component State

```typescript
// ChatMainContainer.tsx
const [selectedFiles, setSelectedFiles] = useState<SelectedFile[]>([]);
const [isUploading, setIsUploading] = useState(false);

// SelectedFile interface
interface SelectedFile {
  id: string; // UUID
  file: File;
  preview?: string; // Blob URL for images
}
```

### Upload Mutation State

```typescript
const uploadMutation = useUploadFilesBatch({
  onSuccess: (result: BatchUploadResult) => {
    // Handle success/partial success
    const successfulFiles = result.results
      .filter((r) => r.success)
      .map((r) => ({
        fileId: r.fileId!,
        fileName: r.fileName,
        fileSize: r.size!,
        contentType: r.contentType!,
      }));

    if (successfulFiles.length > 0) {
      sendMessage({ attachments: successfulFiles });
    }

    // Show toast
    if (result.successCount === result.totalFiles) {
      toast.success("Files uploaded successfully");
    } else {
      toast.warning(
        `${result.successCount}/${result.totalFiles} files uploaded. ${result.failedCount} failed.`
      );
    }
  },
  onError: (error) => {
    toast.error("Upload failed. Please try again.");
  },
});
```

---

## ⏳ PENDING DECISIONS

| #   | Decision                         | Impact on Flow                      | Status       |
| --- | -------------------------------- | ----------------------------------- | ------------ |
| 1   | Max files: 10                    | Validation step in FLOW-01          | ✅ Confirmed |
| 2   | Batch size: 50MB                 | Validation step in FLOW-01          | ✅ Confirmed |
| 3   | Auto retry network errors: Yes   | FLOW-08 retry logic                 | ✅ Confirmed |
| 4   | Show per-file progress: No       | Skip progress updates in FLOW-04    | ✅ Confirmed |
| 5   | Retry failed files: No (Phase 3) | FLOW-07 doesn't include retry logic | ✅ Confirmed |

> ✅ All decisions confirmed - Flow complete

---

## 📋 IMPACT SUMMARY

### Hooks to Create:

- `useUploadFilesBatch.ts` - Mutation hook for batch upload
- (Reuse `useSendMessage.ts` - already supports attachments array)

### Functions to Create:

- `uploadFilesBatch()` in `files.api.ts` - API client
- `validateFileSelection()` in `fileHelpers.ts` - Validation logic
- `processUploadResults()` in `fileHelpers.ts` - Parse BatchUploadResult

### State Updates:

- `selectedFiles: SelectedFile[]` - Replace single file state
- `isUploading: boolean` - Disable send button during upload

---

## ✅ HUMAN CONFIRMATION

| Hạng mục                 | Status       |
| ------------------------ | ------------ |
| Đã review flows          | ✅ Đã review |
| Đã verify error handling | ✅ Đã verify |
| Đã verify retry logic    | ✅ Đã verify |
| **APPROVED để tiếp tục** | ✅ APPROVED  |

**HUMAN Signature:** MINH - ĐÃ DUYỆT  
**Date:** 2026-01-14

> ✅ **APPROVED: Tiến hành tạo implementation plan**

---

## 🔗 References

- [Requirements](./01_requirements.md) ✅ APPROVED
- [Wireframe](./02a_wireframe.md) ⏳ PENDING
- [Batch Upload API Contract](../../../api/file/batch-upload/contract.md) ✅ READY
- [Send Message API Contract](../../../api/chat/message-send-with-multiple-attachments/contract.md) ✅ READY
