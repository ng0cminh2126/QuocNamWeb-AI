# [BƯỚC 2B] Conversation Category Management System - User Flows

**Feature:** Conversation Category Management System  
**Module:** Chat - Management API Integration  
**Status:** ⏳ PENDING HUMAN APPROVAL  
**Version:** 1.0  
**Created:** 2026-01-16

---

## 📋 Overview

This document defines all user flows and navigation paths for the Conversation Category Management System. It covers interactions for three user personas:
- **Admin** - Full access to all features
- **Leader** - Can manage members and templates
- **Member** - View-only access

---

## 👥 User Personas

### 1. Admin User
**Permissions:**
- ✅ View all categories and groups
- ✅ Create/edit/delete categories (future - not in this scope)
- ✅ Manage group members (add/remove/promote)
- ✅ Manage checklist templates
- ✅ Navigate to work type editor

**Role Detection:** Hardcoded in UI as `currentUserRole = 'Admin'`

---

### 2. Leader User
**Permissions:**
- ✅ View categories and groups they belong to
- ✅ Manage members of their groups (add/remove/promote)
- ✅ Manage checklist templates for their groups
- ✅ Navigate to work type editor

**Role Detection:** Hardcoded in UI as `currentUserRole = 'Leader'`

---

### 3. Member User
**Permissions:**
- ✅ View categories and groups they belong to
- ❌ Cannot manage members
- ❌ Cannot manage checklist templates
- ✅ Can navigate to work type editor (view-only in future phase)

**Role Detection:** Hardcoded in UI as `currentUserRole = 'Member'`

---

## 🔄 Flow 1: Admin/Leader - View Categories and Groups

**Persona:** Admin, Leader  
**Entry Point:** Sidebar menu item "Quản lý Loại Việc"

```
┌─────────────────────────────────────────────────────────────┐
│ START: User clicks "Quản lý Loại Việc" in sidebar          │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 1] Category List (GroupSelector)                   │
│ - Show loading skeleton                                     │
│ - API: GET /api/categories                                  │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Display categories│   │ Show error message    │
    │ + search          │   │ [Retry] button        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                │                       └─────► [Retry] → Back to API call
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ User actions available:                                     │
│ • Search/filter categories                                  │
│ • Click category → Navigate to Group List                  │
│ • Click [X] or [Back] → Close dialog                       │
└─────────────────────────────────────────────────────────────┘
                            │
                    [User clicks category]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 2] Group List per Category                         │
│ - Show loading skeleton                                     │
│ - API: GET /api/categories/{id}/conversations               │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Display groups    │   │ Show error message    │
    │ + metadata        │   │ [Retry] button        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                │                       └─────► [Retry] → Back to API call
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ User actions available:                                     │
│ • Search/filter groups                                      │
│ • Click [← Back] → Return to Category List                 │
│ • Click [Quản lý thành viên] → Flow 2 (Admin/Leader only) │
│ • Click [Loại việc] → Navigate to Work Type Editor         │
│ • Click [X] → Close dialog                                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                        [END]
```

**Decision Points:**
1. **API Error:** Retry or close
2. **Empty Categories:** Show empty state with "Tạo loại việc đầu tiên" (future feature)
3. **Empty Groups:** Show "Không có nhóm nào trong loại việc này"
4. **Role Check:** Show "Quản lý thành viên" button only if `currentUserRole === 'Admin' || 'Leader'`

---

## 🔄 Flow 2: Admin/Leader - Manage Group Members

**Persona:** Admin, Leader  
**Entry Point:** Click "Quản lý thành viên" button in Group List

```
┌─────────────────────────────────────────────────────────────┐
│ START: User clicks "Quản lý thành viên" button             │
│ From: [Screen 2] Group List                                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 3] Group User Management                           │
│ - Show loading skeleton                                     │
│ - API: GET /api/groups/{id}/members                         │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Display members   │   │ Show error message    │
    │ grouped by role   │   │ [Retry] button        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                │                       └─────► [Retry] → Back to API call
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ User actions available:                                     │
│ • Search/filter members                                     │
│ • Click [+ Thêm TV] → Flow 2A (Add Member)                 │
│ • Click [Remove] → Flow 2B (Remove Member)                 │
│ • Click [Promote] → Flow 2C (Promote Member)               │
│ • Click [Quản lý dạng checklist] → Flow 3                  │
│ • Click [← Back] → Return to Group List                    │
│ • Click [X] → Close dialog                                 │
└─────────────────────────────────────────────────────────────┘
```

### Flow 2A: Add Member Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [+ Thêm TV] button                              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Add Member Dialog (AddMemberDialog)               │
│ - Show loading skeleton                                     │
│ - API: GET /api/users (or similar - to fetch available)    │
│ - Filter out existing members                               │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show user list    │   │ Show error message    │
    │ with checkboxes   │   │ [Retry] button        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                │                       └─────► [Retry] → Back to API call
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ User selects members via checkboxes                         │
│ Counter shows: "Đã chọn: N người dùng"                     │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]        [Thêm (N)]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Add members to list immediately                           │
│ - Show loading spinner on added members                     │
│ - API: POST /api/groups/{id}/members (for each member)     │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Remove from │
    │ Members added     │   │ list, show error msg  │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 3]
```

### Flow 2B: Remove Member Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [Remove] button on member card                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Validation checks:                                          │
│ • Is member the Owner? → Block with error message          │
│ • Is member self? → Block with error message               │
│ • Else → Continue                                           │
└─────────────────────────────────────────────────────────────┘
                            │
                    [Validation passed]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Confirmation Dialog                               │
│ "Bạn có chắc muốn xóa thành viên?"                         │
│ - Show member info (name, email)                            │
│ - Warning: "Hành động này không thể hoàn tác"              │
│ - Buttons: [Hủy] [Xóa]                                     │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]         [Xóa]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Remove member from list immediately                       │
│ - API: DELETE /api/groups/{id}/members/{userId}            │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Add back to │
    │ Member removed    │   │ list, show error msg  │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 3]
```

### Flow 2C: Promote Member Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [Promote to Admin] button on member card       │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Validation checks:                                          │
│ • Is member already Admin/Owner? → Block                    │
│ • Is current user Admin/Owner? → Allow                      │
│ • Else → Block                                              │
└─────────────────────────────────────────────────────────────┘
                            │
                    [Validation passed]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Confirmation Dialog                               │
│ "Promote thành viên lên Admin?"                            │
│ - Show member info (name, email)                            │
│ - Show permissions list                                     │
│ - Buttons: [Hủy] [Promote]                                 │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]       [Promote]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Update role badge to ADMIN immediately                    │
│ - Move member to Admins section                             │
│ - API: POST /api/groups/{id}/members/{userId}/promote      │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Revert role │
    │ Member promoted   │   │ & position, show error│
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 3]
```

---

## 🔄 Flow 3: Admin/Leader - Manage Checklist Templates

**Persona:** Admin, Leader  
**Entry Point:** Click "Quản lý dạng checklist" button in Group User Management

```
┌─────────────────────────────────────────────────────────────┐
│ START: User clicks "Quản lý dạng checklist" button         │
│ From: [Screen 3] Group User Management                     │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 4] Checklist Template Management                   │
│ - Show loading skeleton                                     │
│ - API: GET /api/checklist-templates?conversationId={id}    │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Display templates │   │ Show error message    │
    │ filtered by group │   │ [Retry] button        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                │                       └─────► [Retry] → Back to API call
                │
                ▼
┌─────────────────────────────────────────────────────────────┐
│ User actions available:                                     │
│ • Search/filter templates                                   │
│ • Click [+ Thêm] → Flow 3A (Add Template)                  │
│ • Click [Edit] → Flow 3B (Edit Template)                   │
│ • Click [Delete] → Flow 3C (Delete Template)               │
│ • Click [Đặt làm mặc định] → Flow 3D (Set Default)        │
│ • Click [Đóng] → Return to Group User Management           │
└─────────────────────────────────────────────────────────────┘
```

### Flow 3A: Add Template Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [+ Thêm] button                                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Add/Edit Template Dialog                          │
│ - Input: Template name                                      │
│ - Checklist items editor (add/remove/reorder)               │
│ - Checkbox: "Đặt làm mặc định"                             │
│ - Buttons: [Hủy] [Lưu]                                     │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]         [Lưu]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Validation:                                                 │
│ • Template name not empty? → Continue                       │
│ • At least 1 checklist item? → Continue                    │
│ • Else → Show validation error                              │
└─────────────────────────────────────────────────────────────┘
                            │
                    [Validation passed]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Add template to list immediately                          │
│ - Show loading spinner                                      │
│ - API: POST /api/checklist-templates                        │
│   Body: { name, conversationId, items, isDefault }         │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Update template ID│   │ Rollback: Remove from │
    │ Show success msg  │   │ list, show error msg  │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 4]
```

### Flow 3B: Edit Template Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [Edit] button on template card                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Add/Edit Template Dialog (pre-filled)             │
│ - Load existing template data                               │
│ - Allow editing name, items, default status                 │
│ - Buttons: [Hủy] [Lưu]                                     │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]         [Lưu]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Update template in list immediately                       │
│ - Show loading spinner                                      │
│ - API: PUT /api/checklist-templates/{id}                    │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Revert data │
    │ Template updated  │   │ show error msg        │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 4]
```

### Flow 3C: Delete Template Sub-Flow

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [Delete] button on template card               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Dialog] Confirmation Dialog                               │
│ "Bạn có chắc muốn xóa template này?"                       │
│ - Show template name                                        │
│ - Warning: "Hành động này không thể hoàn tác"              │
│ - Buttons: [Hủy] [Xóa]                                     │
└─────────────────────────────────────────────────────────────┘
                │
        ┌───────┴───────┐
        │               │
        ▼               ▼
    [Cancel]         [Xóa]
        │               │
        └──► Close      │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Remove template from list immediately                     │
│ - API: DELETE /api/checklist-templates/{id}                 │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Add back to │
    │ Template deleted  │   │ list, show error msg  │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 4]
```

### Flow 3D: Set Default Template

```
┌─────────────────────────────────────────────────────────────┐
│ User clicks [Đặt làm mặc định] button                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Optimistic UI Update:                                       │
│ - Set star icon to filled for this template                │
│ - Unset star for previous default template                 │
│ - API: PUT /api/checklist-templates/{id}                    │
│   Body: { ...template, isDefault: true }                   │
└─────────────────────────────────────────────────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
    ┌───────────────────┐   ┌───────────────────────┐
    │ SUCCESS           │   │ ERROR                 │
    │ Show success msg  │   │ Rollback: Revert star │
    │ Default updated   │   │ states, show error    │
    └───────────────────┘   └───────────────────────┘
                │                       │
                └───────────┬───────────┘
                            ▼
                    [Return to Screen 4]
```

---

## 🔄 Flow 4: Member - View Only Access

**Persona:** Member  
**Entry Point:** Sidebar menu item "Quản lý Loại Việc"

```
┌─────────────────────────────────────────────────────────────┐
│ START: Member user clicks "Quản lý Loại Việc"              │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 1] Category List (Same as Flow 1)                  │
│ - No "Thêm" button (future feature)                        │
│ - Can view and search                                       │
└─────────────────────────────────────────────────────────────┘
                            │
                    [User clicks category]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ [Screen 2] Group List per Category                         │
│ - No "Quản lý thành viên" button (role check failed)       │
│ - Only [Loại việc] button visible                          │
│ - Can view group info, unread count, last message          │
└─────────────────────────────────────────────────────────────┘
                            │
                    [User clicks Loại việc]
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Navigate to Work Type Editor                                │
│ (View-only mode in future phase)                            │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
                        [END]
```

**Member Limitations:**
- ❌ Cannot see "Quản lý thành viên" button
- ❌ Cannot see "Quản lý dạng checklist" button
- ❌ Cannot add/edit/delete templates
- ✅ Can view all public group information

---

## 🚨 Error Handling Flows

### Network Error Flow

```
┌─────────────────────────────────────────────────────────────┐
│ API Call Fails (Network error, timeout, 500, etc.)         │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Show error state in component:                              │
│ - Error icon (⚠️)                                          │
│ - Error message (user-friendly)                             │
│ - Technical details (collapsed)                             │
│ - [Retry] button                                            │
└─────────────────────────────────────────────────────────────┘
                            │
                    [User clicks Retry]
                            │
                            ▼
                    [Retry API call]
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
          [Success]                [Failed again]
                │                       │
        Update UI with data      Show error again
                                 + increment retry count
```

### Authentication Error Flow (401)

```
┌─────────────────────────────────────────────────────────────┐
│ API Returns 401 Unauthorized                                │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Clear auth store (token, user data)                         │
│ Show toast: "Phiên đăng nhập đã hết hạn"                   │
│ Redirect to login page                                      │
└─────────────────────────────────────────────────────────────┘
```

### Authorization Error Flow (403)

```
┌─────────────────────────────────────────────────────────────┐
│ API Returns 403 Forbidden                                   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Show error message:                                         │
│ "Bạn không có quyền thực hiện hành động này"               │
│ [Đóng] button                                               │
└─────────────────────────────────────────────────────────────┘
```

### Validation Error Flow (400)

```
┌─────────────────────────────────────────────────────────────┐
│ API Returns 400 Bad Request                                 │
│ Body: { errors: { field: "message" } }                      │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ Show inline errors:                                         │
│ - Highlight invalid input fields in red                     │
│ - Show error message below each field                       │
│ - Keep dialog open for user to fix                          │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔗 Navigation Map

```
Sidebar Menu
    │
    └─► "Quản lý Loại Việc"
            │
            ▼
      ┌─────────────┐
      │ Category    │  ←──────────┐
      │ List        │             │ [Back]
      └─────────────┘             │
            │                     │
      [Click category]            │
            │                     │
            ▼                     │
      ┌─────────────┐             │
      │ Group List  │  ─────────►─┘
      │ per Category│
      └─────────────┘
            │
            ├─► [Quản lý thành viên] (Admin/Leader only)
            │           │
            │           ▼
            │   ┌─────────────────┐
            │   │ Group User      │
            │   │ Management      │
            │   └─────────────────┘
            │           │
            │           ├─► [+ Thêm TV] → Add Member Dialog
            │           ├─► [Remove] → Confirmation Dialog
            │           ├─► [Promote] → Confirmation Dialog
            │           │
            │           └─► [Quản lý dạng checklist]
            │                       │
            │                       ▼
            │               ┌───────────────┐
            │               │ Checklist     │
            │               │ Template Mgmt │
            │               └───────────────┘
            │                       │
            │                       ├─► [+ Thêm] → Add/Edit Dialog
            │                       ├─► [Edit] → Add/Edit Dialog
            │                       ├─► [Delete] → Confirmation Dialog
            │                       └─► [Đặt mặc định] → API call
            │
            └─► [Loại việc]
                    │
                    ▼
            ┌─────────────┐
            │ Work Type   │
            │ Editor      │
            └─────────────┘
```

---

## ⏰ Loading States Flow

### Initial Load Pattern

```
Component Mount
    │
    ▼
Show Skeleton Loader
    │
    ▼
Call API
    │
    ├─► Success → Fade out skeleton, fade in content (300ms)
    │
    └─► Error → Replace skeleton with error state (no animation)
```

### Subsequent Updates (Optimistic UI)

```
User Action (Add/Edit/Delete)
    │
    ▼
Update UI Immediately (Optimistic)
    │
    ▼
Show inline loading spinner on affected item
    │
    ▼
Call API
    │
    ├─► Success → Remove spinner, show success toast (2s)
    │
    └─► Error → Rollback UI change, show error toast (5s)
```

---

## 📱 Responsive Navigation Adjustments

### Mobile (< 640px)

**Changes:**
- Dialogs become full-screen
- Back button in top-left instead of breadcrumbs
- Bottom sheet for confirmations
- Swipe gestures for navigation (future enhancement)

### Tablet (640px - 767px)

**Changes:**
- Dialogs at 90% width
- Sidebar collapsed by default
- Touch-friendly button sizes (min 44px)

---

## ⏳ PENDING DECISIONS (HUMAN ACTION REQUIRED)

| # | Decision | Options | HUMAN Decision |
|---|----------|---------|----------------|
| 1 | Animation duration for UI updates? | 150ms (fast) / 300ms (smooth) / None | ⬜ None |
| 2 | Success toast auto-close time? | 2s, 3s, 5s | ⬜ default |
| 3 | Error toast auto-close time? | 5s, 10s, Manual close only | ⬜ default |
| 4 | Maximum retry attempts for failed APIs? | 3, 5, Unlimited | ⬜ no retry when failed, and error Toast out the body of the Error response if exists |
| 5 | Should member view have read-only Work Type Editor? | Yes (future) / No (block access) | Member cannot see the UI |

---

## 📋 IMPACT SUMMARY

### New Features Added to Flows:
- Optimistic UI updates for all mutations
- Role-based navigation (Admin/Leader vs Member)
- Error recovery flows with retry logic
- Validation flows for user inputs

### User Experience Improvements:
- Immediate feedback on user actions
- Clear error messages with recovery options
- Confirmation dialogs for destructive actions
- Loading states for all async operations

---

## ✅ HUMAN CONFIRMATION

| Item | Status |
|------|--------|
| Đã review tất cả user flows | ⬜ rồi|
| Đã xác nhận role-based logic | ⬜ rồi |
| Đã điền Pending Decisions | ⬜ rồi |
| Đồng ý với error handling | ⬜ ĐỒNG Ý |
| Đồng ý với optimistic UI pattern | ⬜ ĐỒNG Ý |
| **APPROVED để thực thi** | ⬜ APPROVED |

**HUMAN Signature:** Khoa
**Date:** 16012026

> ⚠️ **CRITICAL: AI KHÔNG ĐƯỢC tạo API contract (BƯỚC 3) nếu mục "APPROVED để thực thi" = ⬜ CHƯA APPROVED**
